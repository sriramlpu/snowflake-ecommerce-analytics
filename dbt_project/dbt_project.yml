name: ecommerce_analytics
version: '1.0.0'
config-version: 2

profile: ecommerce_analytics

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
    - "dbt_packages"

    models:
      ecommerce_analytics:
          staging:
                +schema: STAGING
                      +materialized: view
                          marts:
                                +schema: MARTS
                                      +materialized: table
                                            core:
                                                    +materialized: table
                                                          marketing:
                                                                  +materialized: table
                                                                        finance:
                                                                                +materialized: table

                                                                                vars:
                                                                                  start_date: '2016-01-01'
                                                                                    end_date: '2018-12-31'
